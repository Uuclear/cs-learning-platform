# 挑战2：电商订单分析

## 难度
⭐⭐⭐

## 描述
模拟一个电商订单数据库，并编写复杂的分析查询。

要求：
1. 创建用户表（users）、商品表（products）、订单表（orders）
2. 插入模拟数据
3. 编写查询：找出消费金额最高的前3名用户
4. 编写查询：统计每个商品类别的总销售额

## 提示
- 使用多个 `JOIN` 关联三张表
- 使用 `SUM()`、`COUNT()` 等聚合函数
- 使用 `ORDER BY` 和 `LIMIT` 排序取前N名
