# 挑战 2：优化 WebAssembly 性能对比模拟器

## 背景
在 `example-02-perf-comparison.py` 中，我们模拟了 JavaScript 和 WebAssembly 的性能差异，使用矩阵乘法作为基准测试。

## 任务
改进这个性能对比模拟器，使其更真实地反映实际的 WebAssembly 性能优势。

## 要求

### 功能要求
1. **实现真实的算法**：选择一个更适合展示 Wasm 优势的算法，如：
   - 快速傅里叶变换 (FFT)
   - 图像卷积操作
   - 加密算法（如 SHA-256）
   - 物理引擎计算

2. **添加内存访问模式**：模拟 WebAssembly 的线性内存模型
   - 创建一个共享的内存缓冲区
   - 实现内存读写操作
   - 展示缓存局部性的优势

3. **改进基准测试**：
   - 添加预热轮次以消除 JIT 影响
   - 多次运行取平均值
   - 测量内存使用情况
   - 生成性能报告图表（可选）

4. **实现类型安全版本**：
   - 使用 Python 的类型注解
   - 在 "Wasm 风格" 实现中假设所有输入都是正确的类型
   - 在 "JS 风格" 实现中进行运行时类型检查

### 示例场景
实现一个图像模糊算法：

```python
def js_style_blur(image_data, width, height, kernel_size):
    # 包含边界检查、类型验证、动态内存分配
    pass

def wasm_style_blur(image_data, width, height, kernel_size):
    # 假设输入有效，直接进行计算，使用预分配内存
    pass
```

## 提示
- WebAssembly 的主要优势在于：静态类型、编译时优化、线性内存、SIMD 支持
- 考虑数值计算密集型任务，而不是 I/O 密集型任务
- 可以使用 NumPy 来模拟向量化操作（如果可用），但要提供纯 Python 备选方案

## 评估标准
- [ ] 选择的算法能有效展示 Wasm 的性能优势
- [ ] 内存访问模式合理，体现了缓存局部性的重要性  
- [ ] 基准测试方法科学，结果可靠
- [ ] 代码结构清晰，包含详细的中文注释和文档字符串
- [ ] 提供完整的测试用例和性能分析

## 扩展挑战（可选）
- 实现简单的 SIMD 操作模拟
- 添加垃圾回收压力测试（创建大量临时对象 vs 重用内存）
- 比较不同数据类型（整数 vs 浮点数）的性能差异