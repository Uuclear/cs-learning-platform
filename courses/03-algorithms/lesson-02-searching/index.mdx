---
id: '03-02'
title: '查找算法：大海捞针的艺术'
module: '03-algorithms'
difficulty: 2
duration: 20
prerequisites: ['02-01', '02-07']
---

# 查找算法：大海捞针的艺术

> 想象一下，你在一堆袜子中找你的另一只。如果袜子堆是乱放的，你只能一只一只翻；但如果它们按颜色排好队，你就能"猜"着找——这就是查找算法的精髓。

---

## 开场白

同学们好！今天我们要讨论的是一个**每天都在发生，但你可能从未认真思考过**的问题——**查找**。

先问大家几个问题：

1. **你有没有在字典里翻找过单词？** 如果你从第一页开始一页一页翻，翻到"zebra"的时候，你估计头发还剩几根？

2. **玩"猜数字"游戏时，你会怎么猜？** 别人心里想一个1到100的数字，你每次猜完对方说"大了"或"小了"。聪明的玩家第一句一定猜50——为什么？

3. **你的手机通讯录是怎么做到秒找联系人的？** 几千个联系人，点一下字母"张"，所有姓张的人都跳出来了——背后是哪种查找在撑腰？

如果你对这些问题感兴趣，那今天的课就是为你准备的。我们要学的，就是**如何优雅地在数据堆里找到你要的那根针**。

> 💡 **思考问题1**: 如果给你100万个乱序的数字，要找到其中某个数，你会怎么做？

> 💡 **思考问题2**: "猜数字"游戏里，每次猜中间的数字，最多需要几次就能猜到答案？

---

## 核心概念

### 什么是查找？

**查找**（Searching）就是在数据集合中找到满足特定条件的目标元素的过程。

简单来说：**查找就是在一堆东西里找到你要的那个**。

查找无处不在：
- 在通讯录里找朋友的名字
- 在搜索引擎里输入关键词
- 在数据库中查询用户信息
- 在文件管理器里搜索文件
- 甚至...在冰箱里找昨晚剩下的披萨（这也算查找，只不过复杂度是O(n)，因为冰箱里的东西通常很乱）

一个查找算法的好坏，主要看**时间复杂度**——也就是找得快不快。

### 线性查找：朴素的"挨个找"

**算法思想**：从头到尾，一个一个比对，找到了就停下。

就像你在杂乱无章的抽屉里找一把钥匙——你没有别的办法，只能一个格子一个格子翻。

```
[3, 7, 1, 9, 5, 2, 8]  找 5
 ↑     不是
    ↑  不是
       ↑ 不是
          ↑ 找到了！
```

**时间复杂度**：
- **最好情况**：O(1) —— 运气好，第一个就是
- **最坏情况**：O(n) —— 在最后一个，或者压根不存在
- **平均情况**：O(n) —— 平均要查一半的元素

**适用场景**：
- 数据量小（几十个元素以内）
- 数据无序且不值得排序
- 只需要偶尔查找一次

### 二分查找：聪明的"砍一半"

**算法思想**：每次和中间元素比较，然后根据大小关系，**砍掉一半**的搜索范围。

就像玩"猜数字"游戏：
- 范围是1-100，你猜50
- 对方说"小了"，范围缩小到51-100
- 你猜75，对方说"大了"，范围缩小到51-74
- 继续猜中间值……**每猜一次，范围减半！**

```
[2, 5, 8, 12, 16, 23, 38, 56, 72, 91]  找 23
                      ↑
                   mid=4, arr[4]=16 < 23，右边找
                     [23, 38, 56, 72, 91]
                         ↑
                      mid=6, arr[6]=38 > 23，左边找
                         [23]
                          ↑ 找到了！
```

**时间复杂度**：
- **最好情况**：O(1) —— 中间那个就是
- **最坏情况**：O(log n) —— 每次都砍一半，log₂(100万) ≈ 20次！
- **平均情况**：O(log n)

**为什么必须有序？** 因为二分查找依赖"中间元素比目标大，说明目标在左边"这个推理——如果数据无序，这个推理就不成立。就好比字典里的字不是按字母顺序排的，你从中间翻开就毫无意义。

**迭代 vs 递归实现**：
- **迭代**：用while循环，维护left和right指针，更直观
- **递归**：函数自己调用自己，代码更优雅但会消耗调用栈

### 哈希查找：O(1)的终极武器

**算法思想**：用一个哈希函数把目标映射到一个"地址"，直接去那个地址拿数据。

就像去图书馆借书——如果你知道书的编号，直接去对应的书架取就行，不需要一本一本翻。

**时间复杂度**：
- **平均情况**：O(1) —— 理想情况下，一步到位
- **最坏情况**：O(n) —— 哈希冲突严重时，退化成线性查找

**优势**：当数据需要频繁查找时，哈希表是最快的选择。我们在02-07哈希表那节课详细学过它的原理。

---

## 代码演示

### 示例1: 线性查找

让我们看看线性查找的Python实现——简单到几乎不需要解释（但还是要解释）。

```python
# code/examples/example-01-linear-search.py
```

**运行结果**：

```
==================================================
示例1: 线性查找基础
==================================================

列表: [3, 7, 1, 9, 5, 2, 8, 4]

查找 5:
  找到了！在索引 4，比较了 5 次

查找 1:
  找到了！在索引 2，比较了 3 次

查找 10（不存在的元素）:
  没找到目标值，比较了 8 次
```

**代码解释**：
- `linear_search` 是最基础的线性查找，返回目标索引
- `linear_search_with_trace` 展示了查找的每一步，帮助理解过程
- `linear_search_all` 能找出所有匹配位置，适用于有重复元素的场景

### 示例2: 二分查找（迭代+递归）

现在来看二分查找的两种实现方式——迭代和递归。

```python
# code/examples/example-02-binary-search.py
```

**运行结果**：

```
==================================================
示例2: 二分查找（迭代+递归）
==================================================

有序列表: [2, 5, 8, 12, 16, 23, 38, 56, 72, 91]
列表长度: 10

--- 迭代实现 ---

查找 23:
  第1步: left=0, right=9, mid=4, arr[mid]=16
  arr[4]=16 < 23，目标在右边，left 变为 5
  第2步: left=5, right=9, mid=7, arr[mid]=56
  arr[7]=56 > 23，目标在左边，right 变为 6
  第3步: left=5, right=6, mid=5, arr[mid]=23
  找到了！索引=5，共3步
```

**代码解释**：
- `binary_search_iterative` 使用while循环，维护左右边界，适合大多数场景
- `binary_search_recursive` 是递归版本，每次砍一半，代码更简洁
- 示例2-C展示了关键对比：1000个元素中查找，线性需要999步，二分只需10步——快了约100倍！

### 示例3: 二分查找的实际应用

二分查找不只能"找到一个数"，它有很多变种和实际应用。

```python
# code/examples/example-03-binary-search-applications.py
```

**运行结果**：

```
==================================================
示例3: 二分查找的实际应用
==================================================

--- 场景1：重复元素的边界查找 ---
列表: [1, 2, 2, 2, 3, 4, 5, 5, 5, 5, 6]

查找第一个 2 的位置: 1
查找最后一个 2 的位置: 3
查找第一个 5 的位置: 6
查找最后一个 5 的位置: 9
```

**代码解释**：
- `find_first_occurrence` 和 `find_last_occurrence` 能处理有重复元素的情况
- `search_insert_position` 找到元素应该插入的位置，保持有序性
- `count_occurrences` 用两次二分查找统计出现次数，比O(n)的遍历快得多

---

## 深入理解

### 二分查找的工作原理：为什么每次砍一半？

二分查找的核心在于**利用了数据的有序性**。

假设你有一个从小到大排好序的列表，你想找数字23：
1. 先看中间的数，比如是16
2. 因为16 < 23，而列表是有序的，所以**23不可能出现在16的左边**
3. 于是你可以**放心地扔掉左边一半**，只在右边继续找

这就是"信息论"在算法中的体现——**每次比较都获得一个比特的信息**（"在左边"或"在右边"），n个元素中找一个，需要log₂(n)比特信息，所以需要log₂(n)次比较。

### 时间复杂度对比

| 算法 | 平均时间复杂度 | 最坏时间复杂度 | 空间复杂度 | 前提条件 |
|------|---------------|---------------|-----------|---------|
| 线性查找 | O(n) | O(n) | O(1) | 无 |
| 二分查找 | O(log n) | O(log n) | O(1)迭代/O(log n)递归 | 有序 |
| 哈希查找 | O(1) | O(n) | O(n) | 哈希表 |

**数字感受一下**：假设你有**100万个元素**要查找：
- 线性查找：最坏要查 **1,000,000** 次
- 二分查找：最坏只查 **20** 次（log₂(1000000) ≈ 20）
- 哈希查找：平均只查 **1** 次

**结论**：数据量越大，二分查找和哈希查找的优势越明显。

### 三种查找的适用场景

**线性查找**适合：
- 数据量小（<50个元素）
- 数据无序，且只需要查找一次
- 数据频繁变化，不值得每次重新排序

**二分查找**适合：
- 数据量较大（>100个元素）
- 数据已经有序，或者可以预排序
- 需要多次查找（排序一次O(n log n)，之后每次查找O(log n)）

**哈希查找**适合：
- 需要频繁查找
- 对查找速度要求极高
- 可以接受额外空间开销

---

## 实际应用

### 应用场景1: 字典/词典查询

你查字典的时候，用的就是二分查找的思想——不是从第一页开始翻，而是直接翻到大概的位置，然后根据当前页面的字母决定往前翻还是往后翻。

计算机中的字典（哈希表）更是如此：通过哈希函数直接把单词映射到存储位置，O(1)时间查到。Python的`dict`、Java的`HashMap`、C++的`unordered_map`都是哈希表的实现。

### 应用场景2: 数据库索引

数据库之所以能快速查询，核心在于**索引（Index）**——它本质上就是一种有序的数据结构（通常是B+树）。

当你执行 `SELECT * FROM users WHERE id = 123` 时：
- 如果没有索引：数据库只能一行一行扫描（线性查找，O(n)）
- 如果有索引：通过B+树索引直接定位（类似二分查找，O(log n)）

这就是为什么给数据库加索引后，查询速度能从几秒缩短到几毫秒——本质上是从线性查找升级到了对数查找。

### 应用场景3: 版本控制系统中的"二分调试"

Git有一个强大的命令叫 `git bisect`，它用二分查找来**找出引入bug的那次提交**。

假设你的项目有1000次提交，最新代码有bug，但1000次提交前没有bug：
- 手动排查：需要测试1000次
- 用 `git bisect`：只需测试约10次（log₂(1000) ≈ 10）

原理就是二分查找：取中间那次的提交，测试有没有bug，然后砍掉一半的范围，不断缩小，直到找到引入bug的那次提交。

---

## 常见误区

⚠️ **注意这些坑！**

### 误区1: "二分查找在任何情况下都比线性查找快"

**错误理解**：
> 二分查找O(log n)永远比线性查找O(n)快，所以永远用二分查找！

**真相**：
二分查找的前提是**数据有序**。如果数据无序，你需要先排序，排序的时间复杂度是O(n log n)。如果你只需要查找一次，"排序+二分查找"的总时间是O(n log n)，反而比直接线性查找O(n)还慢！

✅ **正确做法**：
- 只查找一次且数据无序 → 线性查找
- 需要多次查找 → 先排序，再用二分查找
- 数据频繁变化 → 线性查找或哈希表

### 误区2: "mid = (left + right) // 2 永远不会溢出"

**错误理解**：
> 计算中间位置，直接(left + right) // 2就行

**真相**：
在Python中不用担心这个问题（Python的整数可以任意大），但在Java、C++等语言中，`left + right` 可能会**整型溢出**（当两个很大的正数相加时）。

✅ **正确做法**：
```python
# Python中没问题
mid = (left + right) // 2

# 在Java/C++中应该写成
mid = left + (right - left) // 2
```

### 误区3: "二分查找只能用于精确匹配"

**错误理解**：
> 二分查找只能找"等于"某个值的元素

**真相**：
二分查找的变体可以做很多事情：找第一个大于目标值的元素（`bisect_right`）、找最后一个小于目标值的元素、找插入位置、统计出现次数……只要数据是有序的，二分查找就能玩出花样。

✅ **正确做法**：
掌握二分查找的常见变体，如"找第一个/最后一个等于target的位置"、"找第一个大于target的位置"等，这些在面试和实际开发中都很有用。

---

## 动手实践

现在轮到你动手了！

### 练习1: 实现线性查找 ⭐

**描述**：
实现一个线性查找函数，能够在一个无序列表中查找目标值，并返回其索引。

**要求**：
- 函数签名：`def linear_search(arr, target) -> int`
- 如果找到，返回目标值的索引
- 如果没找到，返回-1
- 额外功能：记录并打印比较次数

**示例**：
```python
arr = [34, 7, 23, 32, 5, 62]
print(linear_search(arr, 23))  # 输出: 2（比较了3次）
print(linear_search(arr, 100))  # 输出: -1（比较了6次）
```

<details>
<summary>💡 提示（点击展开）</summary>

- 用一个for循环遍历列表
- 用一个计数器记录比较次数
- 比较完后记得打印比较次数

</details>

### 练习2: 实现二分查找 ⭐⭐

**描述**：
实现一个二分查找函数，能够在一个**有序**列表中查找目标值。

**要求**：
- 分别用**迭代**和**递归**两种方式实现
- 函数签名：`def binary_search_iterative(arr, target) -> int` 和 `def binary_search_recursive(arr, target) -> int`
- 如果找到返回索引，没找到返回-1
- 额外功能：打印查找过程（每一步的left、right、mid）

**示例**：
```python
arr = [1, 3, 5, 7, 9, 11, 13, 15]
print(binary_search_iterative(arr, 11))  # 输出: 5
print(binary_search_recursive(arr, 4))   # 输出: -1
```

<details>
<summary>💡 提示（点击展开）</summary>

- 迭代版：维护left和right两个指针，while循环
- 递归版：函数自己调用自己，注意基准情况（left > right时返回-1）
- 中间位置：mid = (left + right) // 2

</details>

### 练习3: 猜数字游戏 ⭐⭐⭐

**描述**：
用二分查找实现一个"猜数字"游戏。程序随机生成一个1到N的数字，玩家每次猜一个数，程序告诉玩家"大了"或"小了"，直到猜中为止。

**要求**：
- 实现电脑猜玩家数字的功能（电脑用二分查找策略）
- 实现玩家猜电脑数字的功能（玩家手动输入）
- 统计两种方式的猜测次数，对比效率
- 验证：N=100时，二分查找最多需要几次？（答案是7次，因为2^7=128>100）

**思考**：
- 如果N=1,000,000，线性查找最多需要多少次？二分查找呢？
- 为什么二分查找如此高效？

<details>
<summary>💡 提示（点击展开）</summary>

- 电脑猜：用二分查找，范围是1到N，每次猜中间值
- 玩家猜：用input()接收输入，根据提示缩小范围
- 对比：N=100时，线性最坏100次，二分最多7次

</details>

---

## 小结与思考

### 本节要点

1. **查找就是在数据中找到目标**——线性查找挨个比对，二分查找每次砍一半，哈希查找一步到位

2. **线性查找 O(n)**——简单但慢，适合小数据和只查一次的场景

3. **二分查找 O(log n)**——快但需要有序，100万元素只要20步，是性价比之王

4. **哈希查找 O(1)**——最快但需要额外空间，频繁查找时的终极武器

5. **选择哪种查找取决于场景**——数据量、是否有序、查找频率，三个维度综合考虑

### 思考题

1. **如果有一个10亿元的有序数组，你要查找一个数。二分查找最多需要多少次比较？** 算一下log₂(10⁹)是多少，感受一下O(log n)的威力。

2. **二分查找一定要用有序数组吗？有没有办法在无序数组上实现类似二分的高效查找？** 想想哈希表和跳表（Skip List）——它们是不是也在用某种"跳跃"策略？

3. **Python的 `in` 操作符（如 `x in list`）用的是哪种查找？** 对于列表它是O(n)，对于集合（set）它是O(1)——为什么同一种语言，同一类操作，复杂度差这么多？

---

## 下节预告

下一节课，我们将学习算法世界中最重要的编程范式之一——**递归：函数的自我修养**。我们将学习：
- 什么是递归，函数如何"自己调用自己"
- 递归的三要素：基准情况、递归情况、向基准靠拢
- 经典递归问题：阶乘、斐波那契数列、汉诺塔
- 递归的代价：调用栈、栈溢出、尾递归优化

敬请期待！

---

## 参考资源

- 《算法导论》（CLRS）第3版，第2章、第12章
- 《算法》（Sedgewick）第4版，第3章：查找
- LeetCode：二分查找相关题目（第704、34、35题）
- 互动演示：[Binary Search Visualization](https://www.cs.usfca.edu/~galles/visualization/Search.html)

---

*本节课由CS教授精心打造，如有疑问欢迎交流！*
