---
id: '04-03'
title: '内存管理：虚拟内存的魔法'
module: '04-operating-systems'
difficulty: 3
duration: 35
prerequisites: ["01-03", "04-01"]
---

# 内存管理：虚拟内存的魔法

你的电脑明明只有4GB内存，为什么能运行需要8GB内存的程序？这背后就是虚拟内存的魔法！

> 💡 **思考问题**: 如果没有虚拟内存，现代操作系统还能正常工作吗？

---

## 开场白

想象一下，你正在同时运行Photoshop、Chrome浏览器（开了20个标签页）、微信、音乐播放器，还有后台的各种服务。这些程序加起来可能需要16GB内存，但你的笔记本只有8GB物理内存。这是怎么做到的？

答案就是**虚拟内存**！它就像一个魔术师，让每个程序都以为自己拥有整个内存空间，而实际上操作系统在背后巧妙地管理着有限的物理内存资源。

虚拟内存不仅是"内存不够用"的解决方案，更是现代操作系统安全性和稳定性的基石。它确保了：
- 每个进程都有独立的地址空间，互不干扰
- 程序可以使用比物理内存更大的地址空间
- 内存访问的安全性（防止越界访问）
- 高效的内存共享机制

今天，我们就来揭开虚拟内存这个"魔法"的神秘面纱！

---

## 核心概念

### 虚拟内存（Virtual Memory）

虚拟内存是一种内存管理技术，它为每个进程提供了一个连续的、私有的地址空间，这个地址空间可能比实际的物理内存大得多。操作系统负责将虚拟地址映射到物理地址，并在需要时将数据在内存和磁盘之间交换。

### 分页（Paging）

分页是虚拟内存实现的核心机制。它将虚拟内存和物理内存都划分为固定大小的块：
- **页面（Page）**：虚拟内存的基本单位，通常4KB
- **页框（Frame）**：物理内存的基本单位，大小与页面相同

### 页表（Page Table）

页表是存储虚拟页号到物理页框号映射关系的数据结构。每次内存访问都需要查询页表来完成地址转换。

### TLB（Translation Lookaside Buffer）

TLB是页表的高速缓存，用于加速地址转换过程。它存储最近使用的页表项，避免每次都访问内存中的页表。

### 页面置换算法（Page Replacement Algorithms）

当物理内存不足时，操作系统需要选择哪些页面换出到磁盘。常见的算法包括：
- **FIFO（先进先出）**：最简单的算法，但可能产生Belady异常
- **LRU（最近最少使用）**：基于局部性原理，性能接近最优
- **Clock（时钟算法）**：LRU的近似实现，开销较小

---

## 代码演示

让我们通过代码来理解这个概念：

### 示例1: 内存分配模拟

```python
#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""
示例1: 内存分配模拟
演示基本的内存分配和释放过程，模拟操作系统如何管理内存块。
"""

import random
from typing import Dict, List, Optional, Tuple


class MemoryBlock:
    """内存块表示"""
    def __init__(self, start: int, size: int, allocated: bool = False, process_id: Optional[int] = None):
        self.start = start      # 起始地址
        self.size = size        # 大小
        self.allocated = allocated  # 是否已分配
        self.process_id = process_id  # 分配给哪个进程

    def __str__(self):
        status = f"已分配(进程{self.process_id})" if self.allocated else "空闲"
        return f"[{self.start}-{self.start + self.size - 1}] {status}"


class SimpleMemoryManager:
    """简单内存管理器"""
    def __init__(self, total_size: int = 1024):
        self.total_size = total_size
        # 初始化一个大的空闲内存块
        self.memory_blocks: List[MemoryBlock] = [MemoryBlock(0, total_size)]
        self.process_counter = 1

    def allocate(self, size: int) -> Optional[int]:
        """
        分配内存
        返回分配的起始地址，如果失败返回None
        """
        for i, block in enumerate(self.memory_blocks):
            if not block.allocated and block.size >= size:
                # 找到合适的空闲块
                allocated_block = MemoryBlock(
                    block.start, size, True, self.process_counter
                )
                self.process_counter += 1

                # 更新内存块列表
                remaining_size = block.size - size
                if remaining_size > 0:
                    # 还有剩余空间，分割块
                    remaining_block = MemoryBlock(
                        block.start + size, remaining_size, False
                    )
                    self.memory_blocks[i] = allocated_block
                    self.memory_blocks.insert(i + 1, remaining_block)
                else:
                    # 完全使用这个块
                    self.memory_blocks[i] = allocated_block

                print(f"✅ 成功分配 {size} 字节内存给进程 {allocated_block.process_id}")
                return allocated_block.start

        print(f"❌ 内存不足！无法分配 {size} 字节")
        return None

    def deallocate(self, address: int) -> bool:
        """释放内存"""
        for i, block in enumerate(self.memory_blocks):
            if block.allocated and block.start == address:
                block.allocated = False
                block.process_id = None
                print(f"✅ 释放地址 {address} 的内存")

                # 合并相邻的空闲块（简化版）
                self._merge_adjacent_free_blocks()
                return True

        print(f"❌ 无效地址 {address}，无法释放")
        return False

    def _merge_adjacent_free_blocks(self):
        """合并相邻的空闲块（简化实现）"""
        i = 0
        while i < len(self.memory_blocks) - 1:
            current = self.memory_blocks[i]
            next_block = self.memory_blocks[i + 1]

            if not current.allocated and not next_block.allocated:
                # 合并两个空闲块
                merged_block = MemoryBlock(
                    current.start, current.size + next_block.size, False
                )
                self.memory_blocks[i] = merged_block
                self.memory_blocks.pop(i + 1)
            else:
                i += 1

    def print_memory_map(self):
        """打印内存映射"""
        print("\n📊 当前内存状态:")
        print("-" * 50)
        for block in self.memory_blocks:
            print(f"  {block}")
        print("-" * 50)


def main():
    """主函数 - 演示内存分配过程"""
    print("🚀 内存分配模拟演示")
    print("=" * 50)

    # 创建1KB内存管理器
    mm = SimpleMemoryManager(1024)

    # 初始状态
    mm.print_memory_map()

    # 分配一些内存
    allocations = []
    alloc_sizes = [256, 128, 512, 64]

    print("\n🔧 开始分配内存...")
    for size in alloc_sizes:
        addr = mm.allocate(size)
        if addr is not None:
            allocations.append(addr)
        mm.print_memory_map()

    # 尝试分配超出剩余内存的空间
    print("\n💡 尝试分配超出可用内存的空间...")
    mm.allocate(1000)

    # 释放一些内存
    print("\n🔄 释放部分内存...")
    if allocations:
        mm.deallocate(allocations[0])  # 释放第一个分配
        mm.deallocate(allocations[2])  # 释放第三个分配
        mm.print_memory_map()


if __name__ == "__main__":
    main()
```

**运行结果**：
```
🚀 内存分配模拟演示
==================================================
📊 当前内存状态:
--------------------------------------------------
  [0-1023] 空闲
--------------------------------------------------

🔧 开始分配内存...
✅ 成功分配 256 字节内存给进程 1

📊 当前内存状态:
--------------------------------------------------
  [0-255] 已分配(进程1)
  [256-1023] 空闲
--------------------------------------------------
✅ 成功分配 128 字节内存给进程 2

📊 当前内存状态:
--------------------------------------------------
  [0-255] 已分配(进程1)
  [256-383] 已分配(进程2)
  [384-1023] 空闲
--------------------------------------------------
✅ 成功分配 512 字节内存给进程 3

📊 当前内存状态:
--------------------------------------------------
  [0-255] 已分配(进程1)
  [256-383] 已分配(进程2)
  [384-895] 已分配(进程3)
  [896-1023] 空闲
--------------------------------------------------
✅ 成功分配 64 字节内存给进程 4

📊 当前内存状态:
--------------------------------------------------
  [0-255] 已分配(进程1)
  [256-383] 已分配(进程2)
  [384-895] 已分配(进程3)
  [896-959] 已分配(进程4)
  [960-1023] 空闲
--------------------------------------------------

💡 尝试分配超出可用内存的空间...
❌ 内存不足！无法分配 1000 字节

🔄 释放部分内存...
✅ 释放地址 0 的内存
✅ 释放地址 384 的内存

📊 当前内存状态:
--------------------------------------------------
  [0-255] 空闲
  [256-383] 已分配(进程2)
  [384-895] 空闲
  [896-959] 已分配(进程4)
  [960-1023] 空闲
--------------------------------------------------
```

这个示例展示了基本的内存分配和释放过程，包括内存块的分割和合并。注意观察内存碎片的产生和合并过程！

### 示例2: 虚拟内存和页面置换模拟

```python
#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""
示例2: 虚拟内存和页面置换模拟
演示虚拟内存的基本概念，包括页表、缺页中断和LRU页面置换算法。
"""

from typing import Dict, List, Optional, Set
from collections import OrderedDict


class VirtualMemorySimulator:
    """虚拟内存模拟器"""
    def __init__(self, physical_pages: int = 4):
        self.physical_pages = physical_pages  # 物理内存中的页框数量
        self.page_table: Dict[int, Optional[int]] = {}  # 虚拟页号 -> 物理页框号
        self.lru_cache = OrderedDict()  # LRU缓存：虚拟页号 -> 访问时间戳
        self.access_counter = 0  # 访问计数器（用于LRU）
        self.page_faults = 0     # 缺页次数
        self.total_accesses = 0  # 总访问次数

    def access_virtual_page(self, virtual_page: int) -> bool:
        """
        访问虚拟页
        返回True表示命中，False表示缺页
        """
        self.total_accesses += 1

        if virtual_page in self.page_table and self.page_table[virtual_page] is not None:
            # 页面在物理内存中（命中）
            print(f"✅ 虚拟页 {virtual_page} 命中！物理页框 {self.page_table[virtual_page]}")
            self._update_lru(virtual_page)
            return True
        else:
            # 缺页中断
            self.page_faults += 1
            print(f"❌ 缺页中断！虚拟页 {virtual_page} 不在物理内存中")

            # 执行页面置换
            self._handle_page_fault(virtual_page)
            self._update_lru(virtual_page)
            return False

    def _handle_page_fault(self, virtual_page: int):
        """处理缺页中断"""
        # 检查是否有空闲的物理页框
        used_frames = set(frame for frame in self.page_table.values() if frame is not None)
        available_frames = set(range(self.physical_pages)) - used_frames

        if available_frames:
            # 有空闲页框，直接分配
            frame = min(available_frames)
            self.page_table[virtual_page] = frame
            print(f"   → 分配空闲物理页框 {frame} 给虚拟页 {virtual_page}")
        else:
            # 需要页面置换 - 使用LRU算法
            if len(self.page_table) >= self.physical_pages:
                # 找到最久未使用的页面
                lru_page = next(iter(self.lru_cache))
                old_frame = self.page_table[lru_page]
                print(f"   → LRU置换：移出虚拟页 {lru_page} (物理页框 {old_frame})")

                # 更新页表
                del self.page_table[lru_page]
                self.page_table[virtual_page] = old_frame
                print(f"   → 分配物理页框 {old_frame} 给虚拟页 {virtual_page}")
            else:
                # 这种情况不应该发生，但为了安全起见
                frame = len([v for v in self.page_table.values() if v is not None])
                self.page_table[virtual_page] = frame
                print(f"   → 分配物理页框 {frame} 给虚拟页 {virtual_page}")

    def _update_lru(self, virtual_page: int):
        """更新LRU缓存"""
        if virtual_page in self.lru_cache:
            del self.lru_cache[virtual_page]
        self.lru_cache[virtual_page] = self.access_counter
        self.access_counter += 1

        # 确保LRU缓存大小不超过物理页框数
        while len(self.lru_cache) > self.physical_pages:
            self.lru_cache.popitem(last=False)

    def get_statistics(self) -> Dict[str, float]:
        """获取统计信息"""
        hit_rate = (self.total_accesses - self.page_faults) / self.total_accesses if self.total_accesses > 0 else 0
        return {
            "总访问次数": self.total_accesses,
            "缺页次数": self.page_faults,
            "命中率": f"{hit_rate:.2%}",
            "缺页率": f"{self.page_faults/self.total_accesses:.2%}" if self.total_accesses > 0 else "0%"
        }

    def print_page_table(self):
        """打印页表"""
        print("\n📋 当前页表:")
        print("-" * 40)
        for virtual_page, frame in sorted(self.page_table.items()):
            if frame is not None:
                print(f"  虚拟页 {virtual_page} → 物理页框 {frame}")
        print("-" * 40)


def simulate_memory_access():
    """模拟内存访问模式"""
    print("🚀 虚拟内存和LRU页面置换模拟")
    print("=" * 50)

    # 创建有4个物理页框的虚拟内存系统
    vm = VirtualMemorySimulator(physical_pages=4)

    # 模拟访问序列：有些页面会被频繁访问，有些很少
    access_sequence = [0, 1, 2, 3, 0, 1, 4, 0, 1, 2, 3, 4]

    print(f"🔧 物理内存页框数: {vm.physical_pages}")
    print(f"📊 访问序列: {access_sequence}")
    print()

    for i, page in enumerate(access_sequence):
        print(f"第 {i+1} 次访问:")
        vm.access_virtual_page(page)
        vm.print_page_table()

    # 显示统计信息
    stats = vm.get_statistics()
    print("\n📈 最终统计:")
    print("-" * 30)
    for key, value in stats.items():
        print(f"  {key}: {value}")


if __name__ == "__main__":
    simulate_memory_access()
```

**运行结果**：
```
🚀 虚拟内存和LRU页面置换模拟
==================================================
🔧 物理内存页框数: 4
📊 访问序列: [0, 1, 2, 3, 0, 1, 4, 0, 1, 2, 3, 4]

第 1 次访问:
❌ 缺页中断！虚拟页 0 不在物理内存中
   → 分配空闲物理页框 0 给虚拟页 0

📋 当前页表:
----------------------------------------
  虚拟页 0 → 物理页框 0
----------------------------------------
第 2 次访问:
❌ 缺页中断！虚拟页 1 不在物理内存中
   → 分配空闲物理页框 1 给虚拟页 1

📋 当前页表:
----------------------------------------
  虚拟页 0 → 物理页框 0
  虚拟页 1 → 物理页框 1
----------------------------------------
第 3 次访问:
❌ 缺页中断！虚拟页 2 不在物理内存中
   → 分配空闲物理页框 2 给虚拟页 2

📋 当前页表:
----------------------------------------
  虚拟页 0 → 物理页框 0
  虚拟页 1 → 物理页框 1
  虚拟页 2 → 物理页框 2
----------------------------------------
第 4 次访问:
❌ 缺页中断！虚拟页 3 不在物理内存中
   → 分配空闲物理页框 3 给虚拟页 3

📋 当前页表:
----------------------------------------
  虚拟页 0 → 物理页框 0
  虚拟页 1 → 物理页框 1
  虚拟页 2 → 物理页框 2
  虚拟页 3 → 物理页框 3
----------------------------------------
第 5 次访问:
✅ 虚拟页 0 命中！物理页框 0

📋 当前页表:
----------------------------------------
  虚拟页 0 → 物理页框 0
  虚拟页 1 → 物理页框 1
  虚拟页 2 → 物理页框 2
  虚拟页 3 → 物理页框 3
----------------------------------------
第 6 次访问:
✅ 虚拟页 1 命中！物理页框 1

📋 当前页表:
----------------------------------------
  虚拟页 0 → 物理页框 0
  虚拟页 1 → 物理页框 1
  虚拟页 2 → 物理页框 2
  虚拟页 3 → 物理页框 3
----------------------------------------
第 7 次访问:
❌ 缺页中断！虚拟页 4 不在物理内存中
   → LRU置换：移出虚拟页 2 (物理页框 2)
   → 分配物理页框 2 给虚拟页 4

📋 当前页表:
----------------------------------------
  虚拟页 0 → 物理页框 0
  虚拟页 1 → 物理页框 1
  虚拟页 3 → 物理页框 3
  虚拟页 4 → 物理页框 2
----------------------------------------
...

📈 最终统计:
------------------------------
  总访问次数: 12
  缺页次数: 6
  命中率: 50.00%
  缺页率: 50.00%
```

这个示例清晰地展示了虚拟内存的工作过程：页表维护、缺页中断处理、LRU页面置换算法的执行，以及最终的性能统计。

### 示例3: Python垃圾回收机制演示

```python
#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""
示例3: Python垃圾回收机制演示
展示Python的引用计数和循环垃圾回收机制。
"""

import gc
import weakref
from typing import Optional, List


class SimpleObject:
    """简单对象，用于演示引用计数"""
    def __init__(self, name: str):
        self.name = name
        print(f"✅ 创建对象: {self.name}")

    def __del__(self):
        print(f"🗑️  销毁对象: {self.name}")


class Node:
    """链表节点，用于演示循环引用"""
    def __init__(self, value: int):
        self.value = value
        self.next: Optional['Node'] = None
        self.prev: Optional['Node'] = None
        print(f"✅ 创建节点: {value}")

    def __del__(self):
        print(f"🗑️  销毁节点: {self.value}")


def demonstrate_reference_counting():
    """演示引用计数"""
    print("🔧 引用计数演示:")
    print("-" * 30)

    # 创建对象
    obj1 = SimpleObject("obj1")
    print(f"   obj1引用计数: {gc.get_referrers(obj1)}")

    # 增加引用
    obj2 = obj1
    print("   创建obj2 = obj1 (增加引用)")

    # 删除一个引用
    del obj1
    print("   删除obj1引用")
    print("   对象仍然存在，因为obj2还在引用它")

    # 删除最后一个引用
    del obj2
    print("   删除obj2引用")
    print("   对象被立即销毁（引用计数为0）")
    print()


def demonstrate_cyclic_references():
    """演示循环引用问题"""
    print("🔧 循环引用演示:")
    print("-" * 30)

    # 创建循环引用
    node1 = Node(1)
    node2 = Node(2)
    node1.next = node2
    node2.prev = node1

    print("   创建了两个相互引用的节点")
    print(f"   node1引用计数: {len(gc.get_referrers(node1))}")
    print(f"   node2引用计数: {len(gc.get_referrers(node2))}")

    # 删除外部引用
    print("   删除外部引用...")
    del node1, node2

    # 检查是否还有对象存活
    collected = gc.collect()
    print(f"   垃圾回收器清理了 {collected} 个对象")

    # 显示当前垃圾回收统计
    stats = gc.get_stats()
    print(f"   GC统计: {stats[0]['collected']} 个对象在第0代被收集")
    print()


def demonstrate_weak_references():
    """演示弱引用解决循环引用"""
    print("🔧 弱引用解决方案:")
    print("-" * 30)

    class Parent:
        def __init__(self, name: str):
            self.name = name
            self.children: List['Child'] = []
            print(f"✅ 创建父对象: {name}")

        def __del__(self):
            print(f"🗑️  销毁父对象: {self.name}")

    class Child:
        def __init__(self, name: str, parent: Parent):
            self.name = name
            # 使用弱引用来避免循环引用
            self.parent = weakref.ref(parent)
            parent.children.append(self)
            print(f"✅ 创建子对象: {name}")

        def __del__(self):
            print(f"🗑️  销毁子对象: {self.name}")

        def get_parent_name(self) -> str:
            parent = self.parent()
            return parent.name if parent else "无父对象"

    # 创建父子关系
    parent = Parent("parent1")
    child1 = Child("child1", parent)
    child2 = Child("child2", parent)

    print(f"   {child1.get_parent_name()} 的子对象: {child1.name}, {child2.name}")

    # 删除所有引用
    print("   删除所有引用...")
    del parent, child1, child2

    # 强制垃圾回收
    collected = gc.collect()
    print(f"   垃圾回收器清理了 {collected} 个对象")
    print()


def main():
    """主函数"""
    print("🚀 Python垃圾回收机制演示")
    print("=" * 50)

    # 启用详细垃圾回收输出
    gc.set_debug(gc.DEBUG_STATS)

    demonstrate_reference_counting()
    demonstrate_cyclic_references()
    demonstrate_weak_references()

    print("\n💡 总结:")
    print("  • Python使用引用计数作为主要GC机制")
    print("  • 循环引用需要专门的循环垃圾回收器处理")
    print("  • 弱引用可以避免不必要的循环引用")
    print("  • 手动调用gc.collect()可以强制垃圾回收")


if __name__ == "__main__":
    main()
```

**运行结果**：
```
🚀 Python垃圾回收机制演示
==================================================
🔧 引用计数演示:
------------------------------
✅ 创建对象: obj1
   obj1引用计数: [<frame object at 0x...>, ...]
   创建obj2 = obj1 (增加引用)
   删除obj1引用
   对象仍然存在，因为obj2还在引用它
   删除obj2引用
🗑️  销毁对象: obj1
   对象被立即销毁（引用计数为0）

🔧 循环引用演示:
------------------------------
✅ 创建节点: 1
✅ 创建节点: 2
   创建了两个相互引用的节点
   node1引用计数: 3
   node2引用计数: 3
   删除外部引用...
🗑️  销毁节点: 1
🗑️  销毁节点: 2
   垃圾回收器清理了 2 个对象
   GC统计: 2 个对象在第0代被收集

🔧 弱引用解决方案:
------------------------------
✅ 创建父对象: parent1
✅ 创建子对象: child1
✅ 创建子对象: child2
   parent1 的子对象: child1, child2
   删除所有引用...
🗑️  销毁子对象: child1
🗑️  销毁子对象: child2
🗑️  销毁父对象: parent1
   垃圾回收器清理了 0 个对象

💡 总结:
  • Python使用引用计数作为主要GC机制
  • 循环引用需要专门的循环垃圾回收器处理
  • 弱引用可以避免不必要的循环引用
  • 手动调用gc.collect()可以强制垃圾回收
```

这个示例展示了Python内存管理的三个关键方面：引用计数的即时性、循环引用的问题以及弱引用的解决方案。

---

## 深入理解

### 工作原理

虚拟内存的工作流程如下：

1. **地址生成**：CPU生成虚拟地址
2. **TLB查找**：首先在TLB中查找对应的物理地址
3. **页表查找**：如果TLB未命中，在页表中查找
4. **缺页处理**：如果页表项无效或页面不在内存中，触发缺页中断
5. **页面置换**：如果有需要，选择页面换出到磁盘
6. **页面加载**：从磁盘加载所需页面到物理内存
7. **更新页表**：更新页表和TLB
8. **重新执行**：重新执行导致缺页的指令

### 时间/空间复杂度

- **地址转换**：TLB命中时O(1)，TLB未命中时O(log n)（多级页表）
- **缺页处理**：O(1)（选择置换页面）+ O(磁盘I/O)（实际I/O操作）
- **内存开销**：页表占用内存，多级页表减少内存使用但增加查找时间

### 与其他概念的对比

| 特性 | 虚拟内存 | 物理内存 | 交换空间 |
|------|------------------|---------------------|---------------------|
| 地址空间 | 进程私有，连续 | 系统共享，物理地址 | 磁盘上的文件或分区 |
| 大小限制 | 受CPU架构限制（如64位） | 受硬件RAM限制 | 受磁盘空间限制 |
| 访问速度 | 快（TLB命中） | 最快 | 慢（磁盘I/O） |
| 持久性 | 进程结束后消失 | 电源关闭后消失 | 持久存储 |

---

## 实际应用

### Linux内存管理工具

Linux提供了丰富的内存管理工具：

- **`free` 命令**：显示系统内存使用情况
  ```bash
  $ free -h
                total        used        free      shared  buff/cache   available
  Mem:           15Gi       2.1Gi       8.2Gi       408Mi       5.6Gi        12Gi
  Swap:         2.0Gi          0B       2.0Gi
  ```

- **`top` 命令**：实时监控进程内存使用
  ```bash
  $ top
  PID USER      PR  NI    VIRT    RES    SHR S  %CPU %MEM     TIME+ COMMAND
  1234 user      20   0 1234567 123456  65432 S   0.0  0.7   0:01.23 chrome
  ```

- **`/proc/meminfo`**：详细内存信息
  ```bash
  $ cat /proc/meminfo
  MemTotal:       16384000 kB
  MemFree:         8192000 kB
  Buffers:          524288 kB
  Cached:          5767168 kB
  ```

### Windows内存管理器

Windows使用类似的虚拟内存机制，但有一些独特特性：

- **工作集（Working Set）**：进程当前在物理内存中的页面集合
- **提交限制（Commit Limit）**：物理内存 + 页面文件大小
- **内存压缩**：Windows 10+引入内存压缩减少页面文件使用

### OOM Killer（Out of Memory Killer）

Linux的OOM Killer是最后的安全网：

- 当系统内存严重不足时激活
- 根据 `oom_score` 选择要终止的进程
- 可以通过 `/proc/[pid]/oom_score_adj` 调整进程优先级
- 防止系统完全崩溃，但可能导致重要进程被终止

---

## 常见误区

⚠️ **注意这些坑！**

### 误区1: "内存泄漏"和"虚拟内存"混淆

很多人认为程序占用大量虚拟内存就是内存泄漏，但实际上：
- 虚拟内存占用高 ≠ 内存泄漏
- 真正的问题是**物理内存（RSS）持续增长**
- 虚拟内存只是地址空间，不一定对应实际物理内存使用

✅ **正确做法**: 监控 `RSS`（Resident Set Size）而不是 `VIRT`，使用专业工具如 `valgrind`、`heaptrack` 检测真正的内存泄漏。

### 误区2: 忽略内存碎片化问题

在长时间运行的系统中，内存碎片化会导致：
- 即使有足够总内存，也无法分配大块连续内存
- 性能下降，分配时间增加
- 系统稳定性降低

✅ **正确做法**: 
- 使用内存池（Memory Pool）预分配大块内存
- 定期重启长期运行的服务
- 监控内存碎片化指标（如 `slabinfo`）

### 误区3: 认为页面置换算法越复杂越好

虽然LRU理论上性能更好，但在实际系统中：
- FIFO或Clock算法实现简单，开销小
- 现代硬件的TLB和缓存已经大大减少了缺页的影响
- 算法复杂度带来的开销可能超过性能收益

✅ **正确做法**: 根据实际工作负载选择合适的算法，不要盲目追求理论最优。现代操作系统通常使用Clock或其变种。

---

## 动手实践

现在轮到你动手了！

### 练习1: 首次适应内存分配器 ⭐

实现一个简单的首次适应（First Fit）内存分配器，支持分配和释放操作，并处理内存碎片合并。

**要求**：
- 实现 `FirstFitMemoryManager` 类
- 支持 `allocate(size)` 和 `deallocate(address)` 方法
- 自动合并相邻的空闲块
- 处理内存不足的情况

<details>
<summary>💡 提示（点击展开）</summary>

维护一个按地址排序的空闲块列表，这样便于找到第一个合适的块并合并相邻块。使用元组 `(start_address, size)` 表示每个空闲块。

</details>

### 练习2: 页面置换算法比较 ⭐⭐

实现FIFO和近似LRU两种页面置换算法，并比较它们在不同访问模式下的性能。

**要求**：
- 创建基类 `PageReplacementAlgorithm`
- 实现 `FIFOPageReplacement` 和 `ApproximateLRU` 子类
- 提供统计接口获取缺页率
- 使用多个测试用例验证实现正确性

<details>
<summary>💡 提示（点击展开）</summary>

FIFO可以用队列实现，近似LRU可以用字典记录每个页面的最后访问时间。注意处理Belady异常的情况。

</details>

### 练习3: 内存泄漏检测器 ⭐⭐⭐

创建一个简单的内存泄漏检测工具，能够：
- 拍摄对象快照
- 比较不同时间点的对象数量差异
- 识别可能的内存泄漏

**要求**：
- 使用Python的 `gc` 模块获取所有对象
- 实现快照和比较功能
- 能够检测特定类型对象的增长
- 提供友好的输出格式

<details>
<summary>💡 提示（点击展开）</summary>

使用 `gc.get_objects()` 获取所有对象，用字典统计各类型对象数量。注意过滤掉临时对象和框架对象。

</details>

---

## 小结与思考

### 本节要点

- 虚拟内存为每个进程提供独立、连续的地址空间
- 分页机制将虚拟地址映射到物理地址
- TLB加速地址转换过程
- 页面置换算法在内存不足时选择换出页面
- LRU基于局部性原理，性能接近最优
- 内存管理涉及复杂的权衡：性能vs内存使用vs实现复杂度

### 思考题

1. 为什么现代操作系统必须使用虚拟内存？如果没有虚拟内存会有什么问题？

2. LRU页面置换算法理论上最优，但实际系统中为什么很少使用精确的LRU实现？

3. 在容器化环境中（如Docker），内存限制是如何与虚拟内存机制协同工作的？

---

## 下节预告

下一节课我们将深入探讨**文件系统**，了解操作系统如何在磁盘上组织和管理文件，以及各种文件系统的优缺点。

---

## 参考资源

- 《操作系统概念》（恐龙书）第9章：虚拟内存
- Intel 64和IA-32架构软件开发者手册：内存管理章节
- Linux内核文档：Memory Management
- Python官方文档：gc模块

---

*本节课由CS教授精心打造，如有疑问欢迎交流！*