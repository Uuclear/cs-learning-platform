{
  "buildCommand": "cd platform && npm run build",
  "outputDirectory": "platform/.next",
  "devCommand": "cd platform && npm run dev",
  "installCommand": "cd platform && npm install",
  "rootDirectory": "."
}
